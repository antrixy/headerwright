# Runbook — FINDING-036 re-verification, 2026-09-20 (third sitting of the day)

**COMMIT THIS FILE BEFORE CHROME OPENS.** Predictions are written below in
advance. The last sitting's registered row E4 falsified a mechanism that three
unregistered reads had supported; that only worked because the prediction was
committed first.

**Counts are NOT written here.** `node test/verify.mjs` prints a `tree:` line.

## Why this sitting exists

FINDING-036 is the last unverified claim standing between this project and a
v0.2.0 tag decision, and **it is in exactly the epistemic position C8 was in
before it fell.**

Its single sighting is C9 sequence 2, 2026-09-19: `requestHeaders[1] =
{X-Forwarded-For, append, bravo}` and `requestHeaders[2] = {X-Forwarded-For,
set, alpha}` produced `bravo` on the wire, where `alpha` was predicted. The
entry states that the setup was verified with `getDynamicRules()` before the
read, "which is what makes the result trustworthy".

**C8 said the same thing, and C8 was wrong.** Both were built in the same
sitting, in the same popup, by the same hand, and the popup has since been
shown to hold two defects that each make a claimed verification unreliable:

- **FINDING-040** — an operation name typed into the value field yields a legal
  entry that silently does something else.
- **FINDING-042** — an in-progress edit discarded when the popup loses focus,
  which is what opening the service worker console does. FIXED and verified
  2026-09-20, but it was live on 09-19.

**042 explains C9 sequence 2 exactly, with no Chrome anomaly required.** If the
second entry never reached storage, only `append bravo` applied, and the wire
reads `bravo`. That is the observed result. It requires the `getDynamicRules()`
claim to be false — which is precisely what C8 turned out to require.

**This is not a claim that 036 is wrong.** Unlike C8, its mechanism is
independently plausible: Chrome's documented precedence says that once a rule
has appended to a header, `set` and `remove` are treated differently, and
`ROADMAP-v0.2plus.md` §v0.2.1 already records it. 036 may well be real. It has
simply never been read twice, and everything else from that sitting that was
read twice did not survive.

## What is NOT being run, and why

The rows R1 and R2 in `test/RUNBOOK-2026-09-20-2-e4-request-side.md` are
**retired, not deferred.** They were written to test whether 035 and 036 share
a mechanism. There is no 035 mechanism left to share. Do not run them.

## Instrument and read method

`test/oracle/server.mjs` on 8787. **Request-side rows are read by NAVIGATION,
not by the Measure buttons** — no client on the page reads request headers.

Navigate to `http://hw.test:8787/echo?case=plain` and read `requestHeaders` in
the JSON body. That field is `req.rawHeaders` (`server.mjs:213`): every header
Chrome actually sent, in order, names unfolded. This is the same instrument and
the same ruling C9 used — `decisions-entry-c9-instrument-and-header.md`.

**Header choice, unchanged from C9 and for the same reason.**
`X-Forwarded-For` is in `APPENDABLE_REQUEST_HEADERS` and is absent from a
browser request by default, so an `append` cannot be confounded by a value
Chrome supplied.

## Before Chrome

- [x] `cd ~/headerwright && git pull --ff-only`, then
      `git log -1 --format='%H %ad'`. Record the sha AND its date.
      **Done.** `988a1c7256d3515663d9649e0830d3549e435395`. Pull range
      `1fe188b..988a1c7`, five files.
- [x] `node test/verify.mjs` → seven PASS. Quote the `tree:` line. Expect
      **466 checks**. A lower number means this runbook's tree is not running.
      **Done.** Seven PASS, `tree: 466 checks, 104 mutation scenarios, 7
      gates`, module-syntax 22 files.
- [x] Restart both instrument servers after the pull (FINDING-037). **Capture
      provenance BEFORE the kill** — `ps -o pid,lstart,command -p <PID>` and
      `lsof -p <PID> -d cwd`. This has now failed three sittings running; if it
      fails a fourth, delete the step rather than keep pretending it is a
      checklist.
      **NOT RESTARTED, DELIBERATELY, AND THAT WAS THE RIGHT CALL.** The pull
      touched `preflight.mjs`, `selftest.mjs`, `mutate-scans.py`, `FINDINGS.md`
      and a runbook — nothing under `test/oracle/` or `test/initiator/`. So the
      running processes could not be stale, and `preflight.mjs` CONFIRMED it by
      digest rather than leaving it assumed: pids 19402 and 19404, both started
      17:30, both `serving the current tree`.
      **This is the step FINDING-037 was fixed to make unnecessary**, and the
      first sitting where a restart was skipped on evidence instead of
      performed on ritual. The provenance sub-step is therefore not "failed a
      fourth time" — it did not arise.
- [x] `node test/preflight.mjs` → **PREFLIGHT PASSES**. Read its new extension
      block (FINDING-041) and compare the printed `name` against the card in
      `chrome://extensions`. They should both read
      `HeaderWright — Modify HTTP Headers`.
      **PREFLIGHT PASSES.** Oracle pid 19402 build `bbf78ddbddf8` 13/13,
      initiator pid 19404 build `1e6dc8fd662c` 12/12, all three hostnames
      resolving to the right pids. Extension block printed
      `HeaderWright — Modify HTTP Headers` / `0.2.0`; the card matched
      exactly.
- [x] If the card differs, press the RELOAD arrow. **Not Remove** — Remove
      destroys `storage.local` and with it every saved profile, including the
      state these rows are built from.
      **Not needed — card matched.** FINDING-041's check answering a question
      by comparison instead of by a precautionary reload, on its first real
      use.
- [x] Service Workers panel on `http://hw.test:8787/` — no registration.
      **Checked. None for this origin**; only the other-origins link.
- [x] Chrome version and OS. Expect 153.0.8010.48 / macOS 26.5.2. A Chrome
      update makes these rows incomparable to C9 and must be recorded loudly.
      **Unchanged from earlier today: 153.0.8010.48 (arm64), macOS 26.5.2
      (25F84), `Profile 15`.** Confirmed at `chrome://version` during the
      second sitting; the browser was not restarted between them, so carried
      forward rather than re-read. The pending update remains untaken.

## The ordering rule, which is new and not optional

**SAVE, then VERIFY, then MEASURE. Never edit between the verify and the
read.**

FINDING-042's fix means an unsaved edit now survives the popup closing — but
the rule still holds, because the verification step is what makes a row
evidence rather than an anecdote, and an edit made after it invalidates it.
Every row below states the sequence explicitly.

---

## S1 — control: does `set` work on this header at all?

Profile `probe`, target `hw.test`. Delete every response entry. Request
entries, exactly one:

1. `X-Forwarded-For` / `req` / `set` / `alpha`

Save. **Then** `chrome.declarativeNetRequest.getDynamicRules()` in the SERVICE
WORKER console, full namespace — a bare `getDynamicRules()` throws
`ReferenceError`, and a row was once pressed anyway on that error. Confirm
`requestHeaders` length 1, `{X-Forwarded-For, set, alpha}`, and NO
`responseHeaders` key.

Then navigate to `http://hw.test:8787/echo?case=plain`.

**Predicted:** `x-forwarded-for: alpha` present exactly once in
`requestHeaders`.

**If this fails, stop.** Nothing below is interpretable if a plain `set` on
this header does not reach the wire, and no row in the corpus has ever
established that it does — C9 only ever measured it in combination.

**Observed: PASSES AS PREDICTED.**

Verified before the navigation: rule 2 `requestHeaders` length 1,
`{X-Forwarded-For, set, alpha}`, no `responseHeaders` key. Rule 1 carries
`X-HW-Legacy` — a different header, so no contention.

`requestHeaders` contained `"x-forwarded-for", "alpha"`, once, after Chrome's
own headers. `"x-hw-legacy", "1"` confirmed both rules live.

**This control had never been run in the project's history**, and S2 rests on
it: without it, S2's result would be equally consistent with "a plain `set` on
this header never reaches the wire at all".

Incidental, and it retires a loose end: `"Accept-Language", "en-US,en;q=0.9"`
is present, which is the baseline the retired R2 row needed and never
established.

---

## S2 — the row that decides FINDING-036

C9 sequence 2, rebuilt. Profile `probe`, request entries, exactly two, in this
order:

1. `X-Forwarded-For` / `req` / `append` / `bravo`
2. `X-Forwarded-For` / `req` / `set` / `alpha`

Set the operation with the **dropdown**. Do not type an operation name into a
value field — that is FINDING-040, and it was done twice on 2026-09-20 by
someone who knew about it.

Save. **Then** `getDynamicRules()` → confirm `requestHeaders` length 2, append
at index 0, set at index 1, both on `X-Forwarded-For`, values `bravo` and
`alpha`. Record it verbatim. **Do not edit anything after this read.**

Then navigate.

**Predicted if FINDING-036 is real:** `x-forwarded-for: bravo`. The later `set`
discarded. Matches C9 sequence 2, and the finding stands with two sightings on
two tree states.

**Predicted if C9 sequence 2 was FINDING-042:** `x-forwarded-for: alpha`. The
`set` applies, both entries were never simultaneously live on 09-19, and
**FINDING-036 is withdrawn** — with it, the last thing blocking the array-order
claim.

**Predicted if something else:** record it and stop. A combined value
(`bravo, alpha` or `alpha, bravo`) means neither reading is right and the
array-order claim needs its own investigation.

**Observed: `bravo`. FINDING-036 REPRODUCES. The 042 explanation is dead.**

`requestHeaders` contained `"x-forwarded-for", "bravo"` — one occurrence, no
`alpha`, no combined value. The `set` at index 1 was discarded.

Verified immediately before the navigation and untouched after: rule 2
`requestHeaders` length 2, `{X-Forwarded-For, append, bravo}` at index 0,
`{X-Forwarded-For, set, alpha}` at index 1.

**Both entries were demonstrably in storage when the request went out**, so the
`set` did not fail to land — Chrome received it and did not apply it. That
rules out FINDING-042 as the explanation for C9 sequence 2, which was the
hypothesis this sitting existed to test.

**FIRST ATTEMPT VOIDED — THE TWO ENTRIES WERE ON DIFFERENT HEADERS.** The
`getDynamicRules()` read showed index 0 on `X-Forwarded-For` and index 1 on
**`X-Forwarded`** — missing `-For`. The popup's header-name field shows about
eight characters, so both rows read `X-Forwa…` on screen and looked identical.

**That near-miss would have read as a CONFIRMATION.** Two entries on different
headers produce `bravo` on `x-forwarded-for` and `alpha` on `x-forwarded` —
and `bravo` is exactly what FINDING-036 predicts. Only the console read caught
it. Raised as FINDING-043: a third distinct way this popup produces a wrong
entry that looks right, after FINDING-040 and FINDING-042.

---

## S3 — consistency check against the surviving record

Only if S2 reproduces `bravo`. If S2 gives `alpha`, skip this: it tests a
record that has just been shown unreliable.

C9 sequence 1, rebuilt. Request entries, exactly two:

1. `X-Forwarded-For` / `req` / `set` / `alpha`
2. `X-Forwarded-For` / `req` / `append` / `bravo`

Save, verify with `getDynamicRules()`, navigate.

**Predicted:** `x-forwarded-for: alpha, bravo`, one header name, values
combined — as C9 sequence 1 recorded.

If S2 reproduces and S3 does not, the 09-19 sitting is internally inconsistent
and every row from it needs re-reading, not just this finding.

**Observed: `alpha, bravo`. MATCHES C9 SEQUENCE 1.**

Verified before the navigation: `{X-Forwarded-For, set, alpha}` at index 0,
`{X-Forwarded-For, append, bravo}` at index 1, both names full.

`requestHeaders` contained `"x-forwarded-for", "alpha, bravo"` — one header
name, values combined in array order. Chrome combined rather than repeating the
name, as C9 recorded.

The 09-19 sitting is internally consistent on the request side. No re-read of
its other rows is forced.

---

## Results

| row | rule shape (one rule, one `requestHeaders` array) | wire |
| --- | --- | --- |
| S1 | `set alpha` alone | `alpha` |
| S2 | `append bravo`, then `set alpha` | **`bravo`** — the `set` discarded |
| S3 | `set alpha`, then `append bravo` | `alpha, bravo` — both applied |

**FINDING-036 is CONFIRMED**, with two sightings on two tree states a day
apart, the second verified in the console immediately before the read and
untouched between.

**The asymmetry is the finding.** Same two operations, same header, same
values; array order alone decides between `bravo` and `alpha, bravo`. S1 is
what makes it clean — a lone `set` on this header works, so the only
difference in S2 is the preceding `append`.

**035 and 036 were never one mechanism, and that is now settled by evidence
rather than by the absence of one.** The request side genuinely suppresses; the
response side never did.

**What actually fails is the project's own claim** that repeated entries on one
header have their array order preserved on the wire. Two of three sequences
honour it; S2 does not. The mechanism is probably documented Chrome behaviour —
`ROADMAP-v0.2plus.md` §v0.2.1 already records that `set` behaves differently on
a header that has been appended to — so this is a CLAIM TO CORRECT, not code
to fix.

## After

- [x] Every Observed block filled, including matches.
- [x] FINDING-036 updated: confirmed, with the 042 explanation recorded as
      tested and rejected.
- [ ] The project's array-order claim ruled on — it is the thing 036 actually
      falsifies, it is stated in the C9 row and nowhere else authoritative,
      and **no fix is needed, only an accurate statement.**
- [x] Export the profile state to `test/fixtures/` and commit it.
      `test/fixtures/2026-09-20-s3-request-side-state.json`, the state at the
      end of S3. sha256 `__S3_STATE_SHA256__`.
- [ ] FINDING-043 raised for the truncated header-name field. It nearly turned
      this sitting into a false confirmation.

## Disposition

**FINDING-040 is now the only remaining v0.2.0 tag blocker**, and it is a
ruling rather than a measurement.

036 is confirmed but is not a defect in HeaderWright: it is Chrome behaviour
the project mis-stated. The correction is a sentence in the C9 row and wherever
the array-order claim is repeated, not a change to `buildRules()`.

**FINDING-043 is new and unranked.** Whether it blocks the tag is an open
question — it produces wrong rules silently, like 040, but unlike 040 it needs
only a header name longer than the field, which is most real header names.
