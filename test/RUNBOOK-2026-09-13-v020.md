# Runbook — v0.2.0 browser sitting, 2026-09-13

> **TWO OF THIS FILE'S CONCLUSIONS WERE OVERTURNED ON 2026-09-20. THE ROWS
> STAND; TWO READINGS OF THEM DO NOT.**
>
> **C8 and C9 sequence 4 — response-side `remove`.** Both are recorded here as
> independent failures of response-side `remove`. FINDING-035 was raised on
> them and has since been **WITHDRAWN**. Four reads on 2026-09-20 removed the
> header in every position tested, including C8's exact shape with the
> prediction committed in advance, and a mis-built entry — operation dropdown
> left on `set`, the intended operation typed into the VALUE field —
> reproduced C8's entire verdict line. Both sightings are now believed to be
> mis-built or never-saved entries. See FINDING-040 and FINDING-042, which are
> the real defects, and `test/RUNBOOK-2026-09-20-2-e4-request-side.md`.
>
> **C9 sequence 2 — `set` after `append`.** This one SURVIVED re-reading. Rows
> S1, S2 and S3 in `test/RUNBOOK-2026-09-20-3-f036.md` reproduced it a day
> later with the stored array verified immediately before the read. FINDING-036
> is confirmed. The alternative explanation — that the second entry never
> reached storage — was tested and rejected.
>
> **Nothing below is edited.** The rows and their Observed blocks are the
> record of what was seen on 09-19, and a record edited to match a later
> conclusion is not a record.

**COMMIT THIS FILE BEFORE CHROME OPENS.** Every row below carries a prediction
written in advance. That is the whole point: an unwritten prediction reshapes
itself to match whatever happens, and this project has a recorded instance of a
session running an hour against a fixture that was never built.

Nothing in v0.2.0 has rendered a pixel. Every automated gate runs in Node
against source and pure functions.

**Counts are NOT written here.** `node test/verify.mjs` prints a `tree:` line
with checks, mutation scenarios and gates. Quote that line into the results,
never a number typed into prose — a hand-maintained total in this very file
said "79 mutants" when the three harnesses held 90. **This sitting is the first
observation of any of it.**

## Before Chrome

- [x] `cd ~/headerwright && node test/verify.mjs` → seven PASS lines.
      Run at `08639fc` after a fast-forward from `0a5e3d6`: the clone was 16
      commits behind main at the start of the sitting. `tree: 440 checks, 104
      mutation scenarios, 7 gates`.
- [x] `/etc/hosts` contains `127.0.0.1 hw.test` and `127.0.0.1 nothw.test`.
      **ABSENT at the start of this sitting.** The "assumed present (sitting
      G)" fixture did not exist on this machine on 2026-09-16; both lines were
      added during the sitting and verified resolving to 127.0.0.1.
      Both are sitting-G fixtures and are deliberately NOT subdomains of each
      other — a grant on one must not reach the other, and that is the whole
      experiment for part B.
- [x] `node test/oracle/server.mjs 8787` and
      `node test/initiator/server.mjs 8790` both running.
- [x] Extension loaded unpacked from `~/headerwright/extension`. **This path is
      permanent.** The extension ID derives from it; moving the directory
      changes the ID and empties storage, which has killed the fixture three
      times.
- [x] Record the extension ID here before starting: `khjeofpciphjaclledledepfppaiicnf`
      **The claim above was false at the start of this sitting.** The only
      HeaderWright installed was unpacked from
      `~/hw/headerwright-52a2eafb83adf8265bfc96ff3b69e1c39dfc8654/extension`,
      ID `nabejgoljpkiimpodnbejocdkdagpcch`. It was DISABLED (not removed, which
      would delete its storage) and a fresh install was loaded from
      `~/headerwright/extension`, giving the ID above with empty storage.
      Chrome 153.0.8010.48, macOS 26.5.2 (arm64), profile path
      `~/Library/Application Support/Google/Chrome/Profile 15` (hw-test).

## Fixtures needed, and what they block

Two are MISSING as of this writing. Rows depending on them are marked and
must be recorded as NOT RUN rather than guessed.

| Fixture | State | Blocks |
| --- | --- | --- |
| `hw.test`, `nothw.test` in hosts file | assumed present (sitting G) | A, B |
| **A v0.1.7 export of a request-only profile**, path + SHA-256 recorded below | **BUILD IN C0** | C4 |
| **A pre-v0.2 stored profile** (created on v0.1.7, not re-saved since) | **BUILD IN C0** | C2 |
| Profile `probe` → target `hw.test`, set `X-HW-Probe: present` | build in row A1 | A, B |
| Wildcard-DNS echo host | **MISSING** | nothing here |
| v0.1.3 install + Chrome profile with no approval history | **MISSING** | nothing here |

---

## Part A — same-origin control

**Run this FIRST and do not skip it.** A cross-origin absence in Part B means
nothing without it: "the initiator grant is missing" and "HeaderWright is not
applying anything at all" produce identical output in Part B, and they call for
opposite responses.

### A1 — build the probe profile

Create profile `probe`: domain `hw.test`, request header `X-HW-Probe` = `present`,
operation `set`. Grant when prompted.

**Predicted:** saves without refusal; one green chip for `hw.test`; status line
reads applying; badge ON.

**Observed:** Saved with no refusal. **No permission prompt appeared** — the
`hw.test` grant taken in C0 was already held and was reused. Green chip for
`hw.test`; status line `2 profiles · 1/1 domain granted · applying 2`; toolbar
badge green `ON`. The count is 2 because `legacy` from C0 also targets
`hw.test`, so both profiles apply there.

### A2 — same-origin request carries the header

Open `http://hw.test:8790/`, press **Measure**.

**Predicted:** verdict reads SAME-ORIGIN; `x-hw-probe: "present"`.
Initiator equals target, so the single grant satisfies both halves of Chrome's
requirement.

**Observed:** **NOT RUN — instrument defective.** `http://hw.test:8790/`
renders, but `GET http://hw.test:8790/index.mjs` returns 404, so the module
never loads and the Measure button has no handler bound. Nothing was measured;
this is not an absent header. See FINDING-033.

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction. Verdict
SAME-ORIGIN, page on `hw.test:8790`, target `hw.test`, initiator origin none
sent. `x-hw-probe: "present"`. `x-hw-second` and `authorization` ABSENT, neither
set by any profile. `allReceivedNames` also carries `x-hw-legacy`, so both
`hw.test`-targeting profiles applied to the one request. `x-forwarded-for`
absent, confirming the default-absence premise the C9 header ruling rests on.
**First measurement either instrument has produced; FINDING-033/034 confirmed
fixed on the wire.**

> If this row fails, **STOP**. Part B is unmeasurable and every later row is
> uninterpretable. Something more basic than HW-V6-01 is wrong.

---

## Part B — the HW-V6-01 measurement

This is the row the response-header oracle **cannot** run. `test/oracle/` is
same-origin by ruling, so initiator equals target there and every row passes
whether or not HeaderWright models initiators at all.

### B1 — cross-origin, only the target granted

Confirm `nothw.test` is NOT granted (no chip for it; check
`chrome://extensions` → Details → Site access).

Open `http://nothw.test:8790/`, press **Measure**.

**Predicted:** verdict reads CROSS-ORIGIN with initiator origin
`http://nothw.test:8790`; **`x-hw-probe: ABSENT`**.

This is HW-V6-01. Chrome requires host permission for the request URL *and* the
initiator for everything except navigations; only the target is granted.

**Observed:** **NOT RUN.** Blocked by the same defect as A2 (FINDING-033), and
uninterpretable regardless: A2 is the control this row depends on.

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction. Verdict
CROSS-ORIGIN, page on `nothw.test:8790`, target `hw.test`, initiator origin
`http://nothw.test:8790`. **`x-hw-probe: ABSENT`.** Precondition verified first:
`chrome://extensions` Site access lists `*://*.hw.test/*` only. Fetch SUCCEEDED
(`servedBy: "hw.test"`, body returned, verdict box neutral) — an absence, not a
failed measurement. `allReceivedNames` carries neither `x-hw-probe` nor
`x-hw-legacy`, so BOTH `hw.test`-targeting profiles failed cross-origin; `origin`
present as expected. **First wire observation of HW-V6-01.**

> **A failed fetch is NOT this result.** The page renders MEASUREMENT FAILED in
> red for that case. Record it as not-run, not as absent — reading a failed
> fetch as a confirmation would manufacture exactly the finding being tested.

### B2 — grant the initiator too

Add `nothw.test` to the `probe` profile's domains. Grant when prompted.
Re-run `http://nothw.test:8790/` → **Measure**.

**Predicted:** `x-hw-probe: "present"`. Both halves now granted.

**Observed:** **NOT RUN.** Same instrument defect (FINDING-033).

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction. Verdict
CROSS-ORIGIN, initiator origin `http://nothw.test:8790`, `x-hw-probe:
"present"`. B1/B2 pair complete. **UNPREDICTED:** `allReceivedNames` also carries
`x-hw-legacy`, from the `legacy` profile, which targets `hw.test` only and was
NOT edited. Absent in B1, present here — granting `nothw.test` via `probe`'s
domain list re-enabled a second, untouched profile's rule cross-origin. The
domain list governs MATCHING; the host grant governs PERMISSION and is
extension-wide. Candidate finding: the popup presents the domain list as if it
scopes that profile.

> B1 absent + B2 present is the decisive pair. **Either result on its own is
> weak**: B1 alone could be any failure; B2 alone could be a same-origin path.

### B3 — does the grant survive a revoke?

Remove `nothw.test` from the profile. Re-run.

**Predicted:** back to `x-hw-probe: ABSENT`.

**Observed:** **NOT RUN.** Same instrument defect (FINDING-033).

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction.
`x-hw-probe: ABSENT`. Fetch succeeded (`servedBy: "hw.test"`). `allReceivedNames`
identical to B1's, `x-hw-legacy` absent again — the B2 effect reverses on revoke,
giving that finding an on/off/on/off pair. The grant did not persist past the
domain removal, so the revocation path is observed rather than assumed.

### B4 — what the popup claims during B1

While in the B1 state, hover the `hw.test` chip.

**Predicted:** tooltip reads *access granted. Headers apply to page loads here,
and to requests made by pages on this or another granted domain.* Green dot.

This is the v0.2.0 interim wording. **The dot is green and the header is not
applying — that is correct and intended.** The dot reports the GRANT. If the
tooltip still says `permission granted, headers apply`, the interim did not
load and the extension is lying on screen.

**Observed:** Run standalone, by ruling, since the popup half is mechanically
independent of the page. Tooltip on the `hw.test` chip, verbatim: *hw.test:
access granted. Headers apply to page loads here, and to requests made by pages
on this or another granted domain.* Green dot. **The interim wording did load.**
**Qualification:** the grant and profile state matched B1's setup, but B1 is NOT
RUN, so this records what the popup claims WITHOUT the paired measurement of
what actually applies. The green-dot-on-a-non-applying-header half of this row
is unverified.

**Qualification DISCHARGED, sitting 2 (2026-09-19):** not re-run; B1 supplies the
missing pairing. B1 measured `x-hw-probe: ABSENT` cross-origin under the same
grant and profile state in which this tooltip was read. So the green dot and the
interim wording did sit on a header that measurably was not applying to the
request under test. The row's claim is now verified on both halves.

---

## Part C

---

## Part C — v0.2.0 response headers

Only after A and B. Uses `test/oracle/` on 8787.

### C0 — build the fixtures C2 and C4 need, BEFORE installing the candidate

Both rows compare against v0.1.7 behaviour, so their artifacts cannot be made
after the new build is loaded.

1. Load the **v0.1.7** build (tag `v0.1.7`) in a separate unpacked directory.
2. Create profile `legacy`: domain `hw.test`, request header `X-HW-Legacy` =
   `1`, operation `set`. This is C2's pre-v0.2 stored profile.
3. Export it. Save as `test/fixtures/v017-request-only.json`.
4. Record `shasum -a 256 test/fixtures/v017-request-only.json`:
   `550eb97820110781c4ba4a5b9e4b36011e80c2dfa41d5fb872bd050578a52250`
5. Load the candidate build and let it read the same storage.

**Predicted:** the export is version 1 and contains no `side` key.

**Observed:** Export is `"version": 1`, one profile `legacy` (`id: 1`), domain
`hw.test`, one header `{name: "X-HW-Legacy", operation: "set", value: "1"}`.
**No `side` key anywhere** — not on the entry, not on the profile, not at top
level. 307 bytes.

**DEVIATION from step 1, by ruling.** A separate unpacked directory yields a
different extension ID and therefore a different `chrome.storage.local`, so the
candidate could never have read the profile built there and C2 would have been
unrunnable. Instead v0.1.7 was loaded AT the permanent path by checking out tag
`v0.1.7` (`52a2eafb83adf8265bfc96ff3b69e1c39dfc8654`) in `~/headerwright`,
building `legacy`, exporting, then `git checkout main` (`08639fc`) and pressing
Reload. Same path, same ID, same storage. Confirmed the candidate was loaded by
`lib/profile.js` appearing in the service worker's Sources panel — that file
exists only on main. **Both builds report version 0.1.7 in `chrome://extensions`;
the manifest is not bumped on main, so the on-screen version cannot identify the
build and the git sha is the only record.**

> Skipping C0 makes C2 and C4 unrunnable. They were written without their
> artifacts in the first draft of this runbook and would have been answered
> from memory.

### C1 — the popup side control renders

Open the editor on a profile. Look at the header row.

**Predicted:** five columns — name, a 56px `req`/`res` select, operation,
value, remove. No wrap at the 380px popup width. Name and value fields about
70px each and visibly tight.

**NEVER RENDERED.** The 70px figure is derived from CSS, not measured. Record
what you actually see, including if it wraps.

**Observed:** Five controls on the row: name, side select, operation select,
value, `×` remove. The side select offers exactly two options, `req` and `res`,
in that order, `req` selected. **No wrap** at the popup width. Name and value
fields are tight enough to truncate: `X-HW-Pr…` for `X-HW-Probe`, and in C4 the
value `rewritten` rendered as `rewritter` — the clipped `n` reads as an `r`,
producing a different word rather than an obviously cut one. No `req`/`res`
column header or caption appears anywhere outside the select. Widths not
measured in px.

### C2 — a 0.1.x profile still reads as request

Open the `legacy` profile from C0.

**Predicted:** every row's side select reads `req`. A sideless entry means
request, and reclassifying one would change what a user's saved configuration
means.

**Observed:** `req`. The `legacy` row reads name `X-HW-Le…`, `req`, `set`, `1`,
`×` — created on v0.1.7 and not re-saved since. Sideless stored entry renders as
request.

### C3 — export is byte-identical for a request-only profile

**RUNS BEFORE ANY RESPONSE ENTRY EXISTS.** Export is whole-set, not
per-profile: one response entry anywhere raises the entire document to version
2. In the first draft this row came after the response entry was added and
could not have passed.

With only request-side profiles present, export and compare:

```
diff <new export> test/fixtures/v017-request-only.json
```

**Predicted:** identical, including `"version": 1`.

**THE ONLY ROW HERE THAT IS MEASURED RATHER THAN EYEBALLED.**

**Observed:** **NOT identical — prediction not met.** `diff` reports `17,30d16`
with every differing line a `<` from the new export: the whole `probe` block
(`id: 2`, `hw.test`, `X-HW-Probe`/`set`/`present`). No `>` lines, so `legacy` is
byte-identical and the fixture contributes nothing the export lacks. `"version":
1` unchanged, no `side` key present.

**Cause is procedural and was known before the sitting opened:** export is
whole-set, `probe` was created in A1, and the fixture predates it. Raised before
Chrome opened; ruled to run where it sits rather than capture early. The codec
behaviour the row exists to test is intact. **Recorded as prediction not met,
cause known, NOT as a codec defect.** New export kept at
`test/fixtures/c3-candidate-export.json`.

### C4 — response entry round trip

Add a response entry: `X-HW-Oracle` = `rewritten`, side `res`, operation `set`.
Save, close the popup, reopen.

**Predicted:** the entry reads `res` after reopening.

**Observed:** Reads `res` after a full close and reopen. The pre-existing
`X-HW-Probe` row still reads `req`. No warning, no permission prompt, status
line unchanged; the profile list went to `2 headers`. Entered on `probe` rather
than `legacy`, so C5's deletion would not land on C0's fixture artifact.

### C5 — export → import → apply stays on the response side

Export. Delete the profile. Re-import. Check the entry is still `res` and
still applies.

**Predicted:** still a response header, and the document is now version 2.

**This row would have FAILED before the codec fix** — `side` was dropped and
the entry became a request header. It is the row nobody thought to write.

**Observed:** Export is `"version": 2`. The `X-HW-Oracle` entry carries
`"side": "response"` and `"value": "rewritten"`; `X-HW-Probe` and `legacy` carry
**no `side` key at all**, so sideless-means-request holds in the serialized form
and only response entries are annotated. Stored token is `response`; the UI
renders `res`. Delete confirmed via *Delete "probe" and its 1 domain? This
cannot be undone.*, after which `1 profile · 1/1 domain granted · applying 1`.
**Import is replace-all, not merge:** *Replace your 1 profile with 2 profiles
from "headerwright-profiles.json"?* After import, `probe` returns with `id: 2`,
the entry still `res`/`set`, status `2 profiles · 1/1 domain granted ·
applying 2`. **"still applies" is unverified on the wire** — it rests on the
status line, and C7 is NOT RUN. Artifact kept at
`test/fixtures/c5-v2-export.json`.

Incidental, no row covers it — the export surface warns: *Exported to
headerwright-profiles.json. Header values are saved in plain text — if any are
tokens or cookies, treat the file as a secret.* Note it names the requested
filename, not the written one: Chrome wrote `headerwright-profiles (1).json`
because the earlier export still sat in `~/Downloads`.

### C6 — response + append is refused, on screen

Set a header to side `res`, operation `append`. Save.

**Predicted:** refused with a message naming the reason.

**The refusal message has never been read by a human.** Record it verbatim.

**Note the documentation conflict:** `SCOPE.md` currently promises append for
response headers in v0.2. This refusal is a staged product decision, not a
Chrome limitation. Whichever way it settles, SCOPE and the validator must
agree before tagging.

**Observed:** **Refused.** Verbatim, in red, inline in the editor between the
header rows and the buttons:

> Header "X-HW-Append": append is not supported on response headers in this
> release.

Names the offending header and the reason. Not a dialog or a toast; the editor
stays open with the offending row intact, and the status line is unchanged, so
nothing was written. The phrase "in this release" reads as a staged decision
rather than a platform limit — **`SCOPE.md` still promises append for response
headers in v0.2 and must be reconciled with the validator before tagging.**
Editor closed with Cancel so `probe` stayed at two headers.

### C7 — the wire row, on the host the profile actually targets

**Open `http://hw.test:8787/`, not `127.0.0.1`.** The first draft said
`127.0.0.1:8787` while the only defined profile targets `hw.test`.
`requestDomains` does not equate those hostnames, so that row was a guaranteed
false negative — the same "empty diff guaranteed by construction" failure
recorded as FINDING-030 sighting 2.

Run the CORS case with a profile setting `X-HW-Oracle`.

**Precondition:** no service worker registered for `hw.test`, and the fixture
sends `Cache-Control: no-store`. DNR does not affect responses produced by a
page service worker or served from CacheStorage, so a correct rule can look
inert. Unregister via DevTools → Application → Service Workers and record that
you did: **NOT CHECKED — the page never measured, so the precondition was never
reached. Must be done afresh when this row is re-run.**

**Predicted:** MODIFIED, with `X-HW-Oracle` showing the profile's value.

**Predicted for `Set-Cookie`:** NOT OBSERVED. The Fetch spec strips it from
`Response.headers` even same-origin. **No v0.2.0 claim may rest on it.**

**Observed:** **NOT RUN — instrument defective.** `http://hw.test:8787/`
renders, but `GET http://hw.test:8787/index.mjs` returns 404, so both Measure
buttons are inert. Same root cause as A2, in the second instrument. See
FINDING-033. Nothing on the wire was observed, so nothing is recorded for
`X-HW-Oracle` or for `Set-Cookie`.

**Precondition, sitting 2 (2026-09-19):** CHECKED. DevTools → Application →
Service Workers on `http://hw.test:8787/` shows no registration for this origin;
none to unregister. `Cache-Control: no-store` confirmed set on `/echo` and
`/sent` in `test/oracle/server.mjs`.

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction. CORS case,
MODIFIED — 1 changed, 0 removed, 0 added. `x-hw-oracle` sent `baseline`, received
`rewritten`. The three `access-control-*` headers came through unchanged in the
same table, so the diff is targeted rather than wholesale. **First v0.2.0
response-header behaviour ever observed on the wire.** `Set-Cookie`: NOT
OBSERVED, as predicted; neither fixture case emits it and no claim rests on it.

**This row required a server restart first.** `GET http://hw.test:8787/index.mjs`
returned 404 again at the start of the sitting, with `oracle-selfcheck` green.
The process on 8787 (PID 13411) was serving a tree predating `58889bb`;
`server.mjs` is evaluated once at startup, so the `git pull` could not reach it.
Provenance NOT CAPTURED — killed before `ps -o lstart` / `lsof -d cwd`. Candidate
finding: both instrument gates spawn their own server (8788/8789) while the
browser talks to 8787/8790, so a green tree says nothing about the process under
test.

### C8 — response `remove` on the wire

The oracle fixture sends `X-HW-Removable: present`. Add a response entry
removing it.

**Predicted:** MODIFIED, with `X-HW-Removable` shown as removed.

**Response `set` was the only response operation the first draft tested.**
`remove` is in scope for v0.2 and had no wire row at all.

**Observed:** **NOT RUN — instrument defective (FINDING-033).**

**Observed, sitting 2 (2026-09-19): DISAGREES WITH PREDICTION.** Plain case,
MODIFIED — 1 changed, 0 removed, 0 added. **`x-hw-removable` sent `present`,
received `present`** — shown unchanged, not removed. `x-hw-oracle` baseline →
rewritten (C7's `set`, still live, same rule). `x-hw-second` two → two, no rule.

Rule ruled in, not out. `getDynamicRules()` on HeaderWright 0.2.0 /
`khjeofpciphjaclledledepfppaiicnf` returns rule id 2 with `responseHeaders[0] =
{X-HW-Oracle, set, rewritten}` and `responseHeaders[1] = {X-HW-Removable,
remove}` — full name, no truncation, correct operation, no stray value;
`requestDomains: ["hw.test"]`, all fifteen resource types. **Entry 0 applied and
entry 1 did not, on the same request, in the same rule** — which eliminates
matching, domain scoping, resource type, priority, permission and rule
construction, since all are shared between the two entries. Popup offered
`remove` on the `res` side and cleared the value field on selection. Recorded
and stopped; not patched.

### C9 — repeated same-header operation order

The project deliberately allows repeated entries on one header and claims
array order is preserved on the wire. **That claim has never been measured.**

> **2026-09-20: MEASURED, AND THE CLAIM HAD NO AUTHORITATIVE SOURCE.** Rows
> S1/S2/S3 confirmed sequences 1 and 2. But a search of `README.md`,
> `SCOPE.md`, `extension/lib/rules.js` and every decisions entry found the
> claim stated NOWHERE — its only statement is the sentence directly above,
> which attributes it to "the project" without citing it. **A characterisation
> written into a runbook became a fact by being referenced.** The accurate
> behaviour is now documented in `README.md` under "Repeated entries on one
> header", which is the first place a user can read it.

Run each, recording the final wire value rather than the rule JSON:

| Sequence | Predicted final value | Observed |
| --- | --- | --- |
| request `set` A, then `append` B | `A, B` | **`alpha, bravo` — MATCHES** |
| request `append` B, then `set` A | `A` | **`bravo` — DISAGREES** |
| request `append` A, then `append` B | `A, B` | **`alpha, bravo` — MATCHES** |
| response `set` A, then `remove` | absent | **`rewritten` — DISAGREES** |

**NOT RUN — instrument defective (FINDING-033).** Separately, this row does not
name a header or a page for the request-side sequences. The oracle page renders
response headers; the initiator page reports received values only for
`x-hw-probe`, `x-hw-second` and `authorization`. **That must be ruled before the
row is re-run.**

**RULED 2026-09-19, before Chrome opened.** Instrument: the oracle, not the
initiator — `/echo` already returns `requestHeaders: req.rawHeaders`, every
header Chrome sent, same-origin on `hw.test`. Header: `x-forwarded-for`, chosen
for being in `APPENDABLE_REQUEST_HEADERS` AND absent by default, so a `set`-then-
`append` cannot be confounded by a browser default. Read by NAVIGATION to
`http://hw.test:8787/echo`, not the Measure button — no client reads that field.
The old framing had no valid answer: all three request sequences use `append`,
and none of `x-hw-probe`, `x-hw-second` or `authorization` is in the allowlist
(`extension/lib/rules.js` lines 41-47), so `validateHeaderEntry()` refuses them
before Chrome is involved. Full entry:
`handoffs/headerwright/decisions-entry-c9-instrument-and-header.md`.

**Observed, sitting 2 (2026-09-19). Read on `x-forwarded-for` via `/echo`; A =
`alpha`, B = `bravo`. Stored array order verified with `getDynamicRules()` before
each read except seq 4.**

- **Seq 1 — MATCHES.** `x-forwarded-for: "alpha, bravo"`. Name appears ONCE in
  `rawHeaders`: Chrome combined the two operations into one header line rather
  than sending it twice. Read at id `1789854983120`.
- **Seq 2 — DISAGREES.** Predicted `alpha`, observed **`bravo`**. Stored order
  verified as `append bravo` (index 1) then `set alpha` (index 2). The later
  `set` neither overwrote nor combined — it did not apply. Single occurrence in
  `rawHeaders`. Read at id `1789855270022`. Order was changed by editing the two
  rows in place; **the popup offers no reordering control.**
- **Seq 3 — MATCHES.** `x-forwarded-for: "alpha, bravo"`. Stored order verified:
  two `append` entries, `alpha` then `bravo`. Read at id `1789855452306`.
- **Seq 4 — DISAGREES.** `response set rewritten, then remove`, both on
  `X-HW-Oracle` → received **`rewritten`**; predicted absent. CORS case, `1
  changed, 0 removed, 0 added`. Stored order NOT re-verified before this read.

**Seq 2 is the only sequence where a `set` follows an `append`, and the only
request-side failure** — which narrows the shape without theorising.
`ROADMAP-v0.2plus.md` §v0.2.1 already anticipates a precedence interaction here,
so the cause may be documented Chrome behaviour; what fails is the PROJECT's
stated claim that array order is preserved on the wire. **Seq 4 is the second
independent failure of response-side `remove`, after C8** — a header the rule had
just `set`, versus a header the rule had not otherwise touched. Neither applied
while `set` applied in both. Recorded and stopped; not patched.

> A disagreement here is a FINDING about Chrome's evaluation order, not a bug
> to patch. Record it and stop.

### C10 — banner colour reads correctly

**Predicted:** the verdict box is neutral grey, not pink. Red appears only for
MEASUREMENT FAILED.

UNMODIFIED means opposite things in the two phases, so colour deliberately
carries no verdict.

**Observed:** **NOT RUN — instrument defective (FINDING-033).**

**Observed, sitting 2 (2026-09-19): MEASURED — matches prediction, both halves.**
Verdict box neutral grey in every real measurement: A2, B1, B2, B3 (initiator);
C7, C8, C9 seq 4, C11 (oracle) — across presences, absences, MODIFIED results,
and two rows whose result disagreed with its prediction. **Never pink.** Red
observed once, by INDUCED FAILURE (not a runbook step): oracle server stopped
with the page already loaded, then Measure, rendering `MEASUREMENT FAILED —
Failed to fetch. This is not a "no change" result; nothing was observed.` in a
red box. Server restarted afterwards. Note the induced mode was a refused
connection on the fetch, NOT the sitting-1 mode where the module never loaded and
no box rendered at all.

### C11 — hostile header values cannot forge the evidence

Set a response header value to `<b>x</b><script>document.title="forged"</script>`.

**Predicted:** the oracle renders those characters as **text** in the table.
The page title does not change and no bold appears.

The results table used to be built with `innerHTML`, so the profile under test
could write DOM into the page certifying it.

**Observed:** **NOT RUN — instrument defective (FINDING-033).**

**Observed, sitting 2 (2026-09-19):** MEASURED — matches prediction, all three
points. Payload stored BYTE-IDENTICAL — `getDynamicRules()` rule id 2,
`responseHeaders[0].value` = `<b>x</b><script>document.title="forged"</script>`,
neither escaped nor stripped, so the page was genuinely tested rather than handed
something pre-neutered. Table renders the value as literal TEXT; no bold on the
`x`; tab title unchanged at `HeaderWright response-header oracle`. CORS case, `1
changed, 0 removed, 0 added`. The popup accepted the value without refusal or
alteration.

## Optional — testMatchOutcome

`chrome.declarativeNetRequest.testMatchOutcome` may separate "the rule did not
match" from "the rule matched and permission blocked it" — the two failure
modes B1 cannot currently distinguish.

**UNPROVEN. Nothing in this project has used that API.** If it does not behave
as expected, that is a finding about the API, not about HeaderWright, and it
does not affect any row above.

**Observed:** **NOT RUN.** Not reached; no row above depended on it.

**Observed, sitting 2 (2026-09-19): RUN. The API exists and DOES make the
distinction.** `typeof chrome.declarativeNetRequest.testMatchOutcome` =
`'function'` on the unpacked build, no extra permission needed. Same-origin
initiator `http://hw.test:8787` → `matchedRules` = ids 2 and 1, ruleset
`_dynamic`. Cross-origin initiator `http://nothw.test:8790` → **identical
result**, ids 2 and 1.

Read against B1, which measured both headers ABSENT on the wire under that same
initiator: **matching succeeded and permission suppressed application.**
HW-V6-01's mechanism is now confirmed from a second direction that does not touch
the wire. It also firms up B2: rule 1 is `legacy`, targeting `hw.test` only, and
it matched under a `nothw.test` initiator in both runs — matching was never the
variable there either, the grant was.

---

## After

- [x] Fill every Observed line, including NOT RUN where applicable.
- [ ] Commit this file with the results, unedited predictions intact.
- [ ] Any row disagreeing with its prediction becomes a FINDING before it
      becomes a fix.
- [x] `node test/verify.mjs` again — a sitting can leave state behind.
      Green, same `tree:` line. `git status --short` shows only untracked
      `test/fixtures/`. **Seven gates pass over two instruments that cannot
      serve their own JavaScript.**

**What a fully green sitting does and does not establish.** It establishes that
v0.2.0 works as designed and that HW-V6-01 is real and reproducible. It does
**not** establish that option A is correct, that the collision model survives
initiators, or anything about the six missing-fixture rows. Those are v0.3.0's
problem and this sitting must not be read as having settled them.
